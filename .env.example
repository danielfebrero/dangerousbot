# DangerousBot Configuration

# Anthropic API Key (optional - can also be stored in ~/.dangerousbot/secrets/)
ANTHROPIC_API_KEY=sk-ant-...

# Server Configuration
PORT=3000
HOST=localhost

# Environment (development or production)
NODE_ENV=development
